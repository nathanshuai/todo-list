

function Input({ value, onChange }) {
    return (
      <input
        type="text"
        className="result"
        value={value}
        onChange={onChange}
      />
    );
  }
export default Input;